% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability_binomial.R
\name{BIN-reliability}
\alias{BIN-reliability}
\alias{sBIN}
\alias{hBIN}
\alias{mrlBIN}
\title{Threshold-Based Reliability Functions for the Binomial Distribution}
\usage{
sBIN(x, size, prob, log.p = FALSE)

hBIN(x, size, prob)

mrlBIN(x, size, prob)
}
\arguments{
\item{x}{Numeric vector of thresholds or support points.}

\item{size}{Positive integer giving the upper endpoint of the support.}

\item{prob}{Binomial success probability in \eqn{[0,1]}.}

\item{log.p}{Logical; if `TRUE`, `sBIN()` returns logarithmic survival
probabilities.}
}
\value{
`sBIN()` returns the survival probability,
`hBIN()` returns the discrete hazard probability, and
`mrlBIN()` returns the mean residual count.
}
\description{
Computes the discrete survival function, hazard function, and mean
residual count for a binomial random variable.
}
\details{
For \eqn{X \sim Binomial(size,prob)}, define
\deqn{S(x)=P(X\geq x),}
\deqn{h(x)=P(X=x\mid X\geq x),}
and
\deqn{m(x)=E(X-x\mid X\geq x).}
}
\examples{
x <- 0:5
sBIN(x, size = 5, prob = 0.3)
hBIN(x, size = 5, prob = 0.3)
mrlBIN(x, size = 5, prob = 0.3)

}
