% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binomial.R
\name{BIN}
\alias{BIN}
\alias{dBIN}
\alias{pBIN}
\alias{qBIN}
\alias{rBIN}
\title{Binomial Distribution Interface}
\usage{
dBIN(x, size, prob, log = FALSE)

pBIN(q, size, prob, lower.tail = TRUE, log.p = FALSE)

qBIN(p, size, prob, lower.tail = TRUE, log.p = FALSE)

rBIN(n, size, prob)
}
\arguments{
\item{x}{Numeric vector of quantiles.}

\item{size}{Positive integer giving the upper endpoint of the support.}

\item{prob}{Binomial success probability in \eqn{[0,1]}.}

\item{log}{Logical; if `TRUE`, probabilities are returned on the
logarithmic scale.}

\item{q}{Numeric vector of quantiles.}

\item{lower.tail}{Logical; if `TRUE` (default), probabilities are
\eqn{P(X \leq x)}; otherwise, \eqn{P(X > x)}.}

\item{log.p}{Logical; if `TRUE`, probabilities are given or returned
on the logarithmic scale.}

\item{p}{Numeric vector of probabilities.}

\item{n}{Non-negative integer specifying the number of observations
to generate.}
}
\value{
`dBIN()` returns the probability mass function,
`pBIN()` returns the distribution function,
`qBIN()` returns the quantile function, and
`rBIN()` returns a random sample.
}
\description{
Convenience wrappers around the standard binomial distribution functions
provided by base R. These functions are included to provide a common
interface across the count models used by the package.
}
\examples{
dBIN(0:4, size = 4, prob = 0.25)
pBIN(2, size = 4, prob = 0.25)
qBIN(0.5, size = 4, prob = 0.25)

set.seed(123)
rBIN(10, size = 4, prob = 0.25)

}
