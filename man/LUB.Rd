% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lub.R
\name{LUB}
\alias{LUB}
\alias{dLUB}
\alias{pLUB}
\alias{qLUB}
\alias{rLUB}
\title{Lambert-Uniform Binomial Distribution}
\usage{
dLUB(x, size, beta, log = FALSE, tol = 1e-14, maxit = 10000L)

pLUB(
  q,
  size,
  beta,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-14,
  maxit = 10000L
)

qLUB(
  p,
  size,
  beta,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-14,
  maxit = 10000L
)

rLUB(n, size, beta, tol = 1e-14, maxit = 10000L)
}
\arguments{
\item{x}{Numeric vector of quantiles.}

\item{size}{Positive integer giving the upper endpoint of the support.}

\item{beta}{Positive Lambert-uniform parameter.}

\item{log}{Logical; if `TRUE`, probabilities are returned on the
logarithmic scale.}

\item{tol}{Relative tolerance used by the internal evaluation of the
confluent hypergeometric function.}

\item{maxit}{Maximum number of terms used in the hypergeometric series.}

\item{q}{Numeric vector of quantiles.}

\item{lower.tail}{Logical; if `TRUE` (default), probabilities are
\eqn{P(X \leq x)}; otherwise, \eqn{P(X > x)}.}

\item{log.p}{Logical; if `TRUE`, probabilities are given or returned
on the logarithmic scale.}

\item{p}{Numeric vector of probabilities.}

\item{n}{Non-negative integer specifying the number of observations
to generate.}
}
\value{
`dLUB()` returns the probability mass function,
`pLUB()` returns the distribution function,
`qLUB()` returns the quantile function, and
`rLUB()` returns a random sample.
}
\description{
Probability mass function, distribution function, quantile function,
and random generation for the Lambert-uniform binomial (LUB)
distribution.
}
\details{
Let \eqn{P} have survival function
\deqn{
\bar F_P(p;\beta)
=
(1-p)\exp\{(1-\beta)p\},
\qquad 0<p<1,
}
with \eqn{\beta>0}. Its density is
\deqn{
f_P(p;\beta)
=
\exp\{(1-\beta)p\}
[p+\beta(1-p)].
}

Conditionally on \eqn{P=p}, let
\deqn{
X \mid P=p \sim \mathrm{Binomial}(size,p).
}
Then \eqn{X} follows the Lambert-uniform binomial distribution.

The probability mass function is
\deqn{
P(X=x)
=
\frac{1}{size+1}
\left[
\beta\,{}_1F_1(x+1;size+2;1-\beta)
+
(1-\beta)\frac{x+1}{size+2}
{}_1F_1(x+2;size+3;1-\beta)
\right].
}

When \eqn{\beta=1}, the LUB distribution reduces to the discrete
uniform distribution on \eqn{\{0,\ldots,size\}}:
\deqn{
P(X=x)=\frac{1}{size+1}.
}
}
\examples{
dLUB(0:4, size = 4, beta = 3)
pLUB(2, size = 4, beta = 3)
qLUB(0.5, size = 4, beta = 3)

set.seed(123)
rLUB(10, size = 4, beta = 3)

}
