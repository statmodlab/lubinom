% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitLUB_mle.R
\name{fitLUB_mle}
\alias{fitLUB_mle}
\title{Maximum Likelihood Estimation for the LUB Distribution}
\usage{
fitLUB_mle(
  x,
  size,
  start = NULL,
  starts = c(0.05, 0.1, 0.25, 0.5, 1, 2, 5, 10, 20),
  diagnostic_bfgs = TRUE,
  conf.level = 0.95,
  profile.ci = FALSE,
  profile.root.tol = 1e-10,
  maxit = 5000L,
  reltol = 1e-10,
  tol = 1e-14,
  hyp.maxit = 10000L
)
}
\arguments{
\item{x}{Observed bounded counts.}

\item{size}{Positive integer giving the upper endpoint of the support.}

\item{start}{Optional positive starting value for beta used by the
diagnostic BFGS fits.}

\item{starts}{Positive deterministic starting values for diagnostic
multistart BFGS optimization.}

\item{diagnostic_bfgs}{Logical; if `TRUE`, diagnostic multistart BFGS
fits are also computed.}

\item{conf.level}{Confidence level for interval estimation.}

\item{profile.ci}{Logical; if `TRUE`, a profile-likelihood confidence
interval is additionally computed.}

\item{profile.root.tol}{Root-finding tolerance for the profile-likelihood
confidence interval.}

\item{maxit}{Maximum number of iterations for diagnostic BFGS.}

\item{reltol}{Relative tolerance for diagnostic BFGS.}

\item{tol}{Numerical tolerance passed to `dLUB()`.}

\item{hyp.maxit}{Maximum number of terms used by the internal
hypergeometric-series evaluation.}
}
\value{
An object of class `"fitLUB_mle"`.
}
\description{
Fits the Lambert-uniform binomial distribution by maximum likelihood.
Optimization is performed on the unconstrained parameter
\eqn{\eta=\log(\beta)}.
}
\details{
A one-dimensional adaptive Brent search is used as the primary
optimization procedure. Optional multistart BFGS optimization can be
used as a numerical diagnostic.


The primary estimator is obtained by minimizing the negative
log-likelihood over \eqn{\eta=\log(\beta)} using an adaptive Brent
search.

Standard errors are obtained from the observed information on the
eta scale and transformed to the beta scale using the delta method.
The default confidence interval is a transformed Wald interval.

If every observation is zero, no finite interior maximum-likelihood
estimate exists. In that case, the likelihood is maximized in the
limit as \eqn{\beta\to\infty}, and the function returns an error.
}
\examples{
x <- c(0, 0, 1, 0, 2, 1, 0, 0)
fit <- fitLUB_mle(
  x = x,
  size = 4,
  diagnostic_bfgs = FALSE
)
fit

}
